name: Frontend CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      - name: Install
        run: npm ci
      - name: Build
        run: npm run build

  #test:
  #  runs-on: self-hosted
  #  needs: build
  #  steps:
  #    - uses: actions/checkout@v4
  #    - uses: actions/setup-node@v4
  #      with:
  #        node-version: "20"
  #        cache: "npm"
  #    - name: Install
  #      run: npm ci
  #    - name: Build
  #      run: npm run build
  #    - name: Start dev server
  #      run: |
  #        npm run dev -- --host 0.0.0.0 --port 5173 &
  #        npx wait-on http://127.0.0.1:5173 --timeout 60000
  #    - name: Cypress e2e
  #      uses: cypress-io/github-action@v6
  #      with:
  #        browser: electron
  #        headless: true

  deploy:
    runs-on: self-hosted
    needs: test
    if: github.ref_name == 'DevelopmentBranch'
    env:
      JWT_SECRET: ${{ secrets.JWT_SECRET }}
      DATASOURCE_PASS: ${{ secrets.DATASOURCE_PASS }}
    steps:
      - uses: actions/checkout@v4
      - name: Deploy stack (mysql + existing backend image + frontend)
        run: |
          docker compose -p chessplatform up -d --build
          docker ps
